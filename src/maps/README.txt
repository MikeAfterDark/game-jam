Map formatting:
Each 'map' must be in its own folder which contains:
- `_.ogg` file for the actual music
- `metadata.lua` file for indexing/menu interaction data
- `map.lua` for the actual map data
- an optional (but recommended!) background image
